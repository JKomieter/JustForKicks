const { Router } = require("express")
const router = Router();
// will import the controllers


router.post("/save/products/:id", )

router.get("/get/products/:id",)

router.delete("/delete/products/:id",)

router.post("/get/user/:id",)


module.exports = router